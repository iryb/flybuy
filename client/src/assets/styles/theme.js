import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1d1d1d",
    },
    secondary: {
      main: "#f8f1ed",
    },
  },
  typography: {
    fontFamily: "Poppins, Arial",
  },
});
